CREATE TABLE user (
    id IDENTITY(10001),
    title VARCHAR(25),
    firstname VARCHAR(25),
    lastname VARCHAR(25),
    gender VARCHAR(25),
    street VARCHAR(50),
    city VARCHAR(25),
    state VARCHAR(25),
    postcode VARCHAR(4)
);